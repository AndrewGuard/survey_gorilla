<div class="container">
  <div class="all_surveys">
    <h1> All Surveys </h1>
    <ul style="line-height:40px">   
        <% @all_surveys.each do |survey|%>
        <li> <%= survey.title %><a href="/take_survey/<%= survey.id %>" value="take survey" class="take_survey_button">Take Survey</a></li>
        <% end %>
    </ul>
  </div>
</div>
